<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.job_moo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name=".zMyService"
            android:enabled="true"
            android:exported="true"></service>

        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".QuestActivity" />
        <activity android:name=".WriteActivity" />
        <activity
            android:name=".Popup1Activity"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".Popup2Activity"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".Popup6Activity"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".Popup11Activity"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".Popup16Activity"
            android:theme="@android:style/Theme.Dialog" />

        <service
            android:name=".zMyService"
            android:enabled="true"
            android:exported="true">
        </service>
    </application>

</manifest>